<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deb.raf.studentapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudentApp">
        <activity android:name=".ui.studentActivity.StudentsGradesActivity"/>
        <activity android:name=".ui.studentsList.ShowGradesActivity" />
        <activity android:name=".ui.studentsList.AddGradesActivity" />
        <activity android:name=".ui.studentsList.ListStudentsActivity" />
        <activity android:name=".ui.studentActivity.StudentActivity" />
        <activity android:name=".ui.chooseRole.ChooseRoleActivity" />
        <activity android:name=".ui.start.StartActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>